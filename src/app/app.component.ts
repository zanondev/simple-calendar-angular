import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <app-calendar></app-calendar>
  `
})
export class AppComponent {
  title = 'simple-calendar-angular';
}
